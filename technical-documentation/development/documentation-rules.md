# Documentation rules (development)

Applies to docs under `development/`. On overlap with the project master [`../documentation-rules.md`](../documentation-rules.md), **this file wins** for those docs; otherwise the master applies.

- Entry: [`contents.md`](./contents.md). This folder is in-house DX / build notes — not the public operator guide.
- Deployment Tool CLI verbs (including `eip dev`) stay under [`../deployment/deployment-tool/cli/`](../deployment/deployment-tool/cli/contents.md); link once, do not duplicate.
- Public bring-up → [`../deployment/guide.md`](../deployment/guide.md).

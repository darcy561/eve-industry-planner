# Technical rules (development)

Applies when editing development docs or documenting local DX workflows. On overlap with the project master [`../technical-rules.md`](../technical-rules.md), **this file wins**; otherwise the master applies.

- Prefer linking live SoT (stack, Deployment Tool CLI, frontend/backend) over re-describing behaviour here.
- Local bake / `eip dev` mechanics → Deployment Tool CLI docs, not a parallel development-only verb list.

# Backend — eipconfig

## Owns (SoT)

Placeholder — docs for `services/eipconfig` / `services/cmd/eipconfig` when written.

## Does not own

- Operator config apply (`eip sync` / secrets) → [stack/config.md](../../stack/config.md), [stack/secrets.md](../../stack/secrets.md), [deployment/deployment-tool/cli](../../deployment/deployment-tool/cli/contents.md)

## Task map

| I need to… | Read |
|------------|------|
| _(none yet — add rows when topics land)_ | |

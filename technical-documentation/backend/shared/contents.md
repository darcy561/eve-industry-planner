# Backend — shared

## Owns (SoT)

Placeholder — docs for libraries under `services/shared` when written.

## Does not own

- Feature SoT that is exposed via API → prefer [api/](../api/contents.md) with links into shared packages from those topics

## Task map

| I need to… | Read |
|------------|------|
| _(none yet — add rows when topics land)_ | |

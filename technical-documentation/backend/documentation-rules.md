# Documentation rules (backend)

Applies to docs under `backend/`. On overlap with the project master [`../documentation-rules.md`](../documentation-rules.md), **this file wins** for those docs; otherwise the master applies.

- Entry: [`contents.md`](./contents.md). Service folders hold live topics; Owns / task maps stay on `contents.md` files.
- One-hop + current-behaviour from the master apply unless this file adds a stricter local rule.
- Migration / cutover writing for backend work stays in [`../migration-plans/`](../migration-plans/contents.md) until promoted here.

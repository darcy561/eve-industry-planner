# Documentation rules (frontend)

Applies to docs under `frontend/`. On overlap with the project master [`../documentation-rules.md`](../documentation-rules.md), **this file wins** for those docs; otherwise the master applies.

- Entry: [`contents.md`](./contents.md). SPA topics live here; Owns / task maps stay on `contents.md` files.
- One-hop + current-behaviour from the master apply unless this file adds a stricter local rule.
- Migration / cutover writing for frontend work stays in [`../migration-plans/`](../migration-plans/contents.md) until promoted here.
